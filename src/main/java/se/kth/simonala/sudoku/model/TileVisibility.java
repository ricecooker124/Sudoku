package se.kth.simonala.sudoku.model;

/**
 * This enum represents the visibility status for every tile.
 * The visibility is either hidden or shown.
 */
public enum TileVisibility {
    HIDDEN, SHOWN;
}
